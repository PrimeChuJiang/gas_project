# 会话 5（2026-08-11 晚）—— TargetActor 需求 1+2 同课关账

## 开场

续接会话 4（TargetData 需求 1 单测补齐）。用户确认"先解决 1（TargetActor）再解决 2（WaitTargetData Task）"。

## 概念课（UE TargetActor）

- `UGameplayAbilityTargetActor` = "执行目标选择的载体"（Actor 活在场景）：
  ① 选择需要世界（物理空间）② 可能持续多帧 ③ 要视觉反馈 ④ 与 GA 解耦（GA 只认数据）
- Godot 与 UE 的分水岭：Node2D/Node3D 是兄弟类，TargetActor 基类落 **Node**
  （坐标系无关），2D/3D 子类各管查询——坐标系相关全推迟到子类
- 目标过滤两层：物理层（collision_mask，已做 @export）/ 逻辑层（filter Callable，
  本次同课实现）；"碰撞体是世界里物体的身份证，UI 不是"——用户选物理方案的动机

## 设计决策（用户定）

1. 物理点选（非逻辑点选）；meta 桥接（`ENTITY_META := &"entity"` 放基类共享）
2. 持续选择**要做**（推翻我"砍掉"建议）→ 缓存 + selection_changed 信号 + confirm 买定离手
3. 跳变沿判定**全量比较**（actors + location + has_location）——"不能只比一半"；
   前提：location 存"实体位置"（跟实体走）而非鼠标位置（每帧变）
4. location 保持 Vector3（"谁知道多出来的值以后有没有小妙用"——数据层不为眼前收窄）
5. 过滤器：基类 `var filter: Callable = Callable()`，is_valid() 判空、fail-open 默认、
   拒收 = continue 找下一个、全拒 = 空选择
6. select_area（范围多目标）**现在一起做**（用户："没必要等到后面用的时候再回头"）：
   intersect_shape + CircleShape2D + 全量收集进 actors；"取第一个 vs 收全部"差异留循环里，
   `_resolve_entity(result) -> Node` 抽公共过滤链（两处就抽纪律）

## 用户反复踩的坑（教学重点，第四次终于钉死）

**跳变沿判定的"顺序比较"病根**——同一结构错误出现四次变体：
1. 引用比较（old != new 实例）→ 每帧误发
2. 提前 return 吞后比字段（location 相同 return，actors 变化被吞）
3. if-elif 互斥吞分支（都 true + location 同 → actors 永不比）
4. 手改时 elif→if 丢 null 门卫（首次 select_at 崩）
**根治**：思维模型从"分情况处理"换成"找不同"——每个字段独立关卡、任何不同即 false、
绝不提前 return 跳过字段；`is_same_as` 四步：null → has_location → location → actors。

其他：
- 基类 51 行笔误 return false→true（select_area 成功路径）
- **GDScript lambda 值捕获实锤**：`var f := func(): count += 1; f.call()` 后 count 仍为 0
  （本地脚本验证）——lambda 捕获局部变量是快照，改不动外部；回调改状态必须走
  **成员变量 + 命名函数**（self 引用）或容器（数组/字典按引用）。项目 GameplayCue
  计数先例就是正确姿势。**新账，值得进文档。**
- intersect_point 返回 Array[Dictionary]（collider/collider_id/rid/shape），
  无位置字段、无顺序保证；重叠时"取第一个"不可靠 → z 排序记入想法清单
- get_meta 键不存在会 push_error → has_meta 守卫（或带默认值参数）
- Viewport.world_2d 存在（Godot 4.7 文档确认）——Node 也能拿 2D 空间

## 测试（我写，用户实现框架）

- 基础-50~62：TargetData 数据容器单测（上轮）
- 基础-63~90：TargetActor 28 条——点选命中/位置/跳变沿/空地/买定离手/cancel/
  过滤器放行拒收/范围多目标/范围空/过滤器在范围拒收部分/单→多切换/拷贝隔离
- 物理坑：StaticBody2D 挂树后 await physics_frame 再查询
- 首次跑：166/5（5 个信号断言全挂）→ 根因 lambda 值捕获 → 改成员变量+命名回调
  → **171/0 全绿，ExitCode=0**

## 未决

- 需求 2：WaitTargetData Task（把"等玩家选目标"包成 Task）——下次课
- 想法清单：重叠 z 排序、AOE 圈视觉预览、intersect_shape 版多目标已落地（select_area）
